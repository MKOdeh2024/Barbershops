// next.config.js [cite: 6]
/** @type {import('next').NextConfig} */
const nextConfig = {
    reactStrictMode: true,
    // Add other Next.js configurations here
    // E.g., PWA configuration if using packages like next-pwa [cite: 1, 121]
    // images: { domains: ['example.com'] } // Configure image optimization if needed
  };
  
  module.exports = nextConfig;